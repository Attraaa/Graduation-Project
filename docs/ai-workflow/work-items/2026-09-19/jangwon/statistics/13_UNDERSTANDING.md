# 13. 변경사항 이해 워크시트

> 전체 코드를 한 줄씩 이해하려 하지 말고 아래 빈칸을 채운다. 모르는 항목은 `모름`이라고 쓴 뒤 AI에게 질문한다. 주석 속 예시는 참고용이다.

## 1. 무엇을 바꿨나

<!-- 예: 이 변경은 결제 응답이 늦을 때 중복 결제되는 문제를 해결한다. 변경 전에는 timeout을 실패로 봤고, 변경 후에는 확인 중으로 둔다. -->

이 변경은 통계 db 연결 및 학습이력의 db 문제를 해결한다.

변경 전에는 더미(하드코딩) 데이터로 동작했다.

변경 후에는 sqlite 로컬 db로 동작한다.

## 2. 어디서 시작하나

<!-- 예: src/payments/payment.controller.ts / PaymentController.authorize() / 주문 번호 / 결제 버튼을 누른 사용자 -->

- 최초 진입점 파일: 모름
- 최초 함수/클래스: 모름
- 입력: 모름
- 호출 주체: 모름름

## 3. 어떤 경로로 실행되나

<!-- 예: 요청 수신 → 기존 결제 확인 → 외부 결제사 요청 → 상태 저장 → 화면에 결과 반환. 세부 함수보다 책임이 바뀌는 최대 5단계로 쓴다. -->

```text
1. 모름름
→ 2.
→ 3.
→ 4.
→ 5.
```

## 4. 중요한 데이터는 어디서 바뀌나

<!-- 예: 결제 시도 상태 / applyProviderResult() / created / succeeded·rejected·pending / 불명확하면 pending -->

- 변경되는 데이터: 모름름
- 변경 함수:
- 변경 전 상태:
- 변경 후 상태:
- 실패 시 남는 상태:

## 5. 외부 부작용이 있나

<!-- 예: 외부 결제 승인 요청을 보내고 결과를 DB에 저장하므로 DB 쓰기·외부 API·결제를 체크한다. -->

- [ ] DB 쓰기 또는 삭제
- [ ] 외부 API 요청
- [ ] 파일 변경
- [ ] 캐시 변경
- [ ] 이벤트 또는 메시지 발행
- [ ] 이메일·알림·결제
- [ ] 없음

구체적인 내용:


## 6. 가장 위험한 실패는 무엇인가

<!-- 예: 결제사는 승인했지만 서버가 응답을 잃음 / 화면은 확인 중 / DB에는 pending / 재시도해도 새 결제를 만들지 않음 -->

- 실패 상황:
- 사용자에게 보이는 결과:
- 시스템에 남는 상태:
- 재시도 시 결과:

## 7. 무엇으로 검증했나

<!-- 예: timeout 통합 테스트와 타입 검사 / 실제 운영 결제사 장애와 모바일 문구는 확인하지 못함 -->

- 관련 테스트:
- 실행한 검사:
- 테스트가 확인하는 것:
- 테스트가 확인하지 않는 것:

## 8. 문제가 생기면 어떻게 하나

<!-- 예: 기능 플래그 끄기 / 이전 버전 롤백 / pending 지표 확인 / 결제 담당자에게 요청 -->

- 중지 방법:
- 롤백 방법:
- 확인할 로그/지표:
- 도움을 요청할 담당자:

## 9. 내가 아직 모르는 것

<!-- 예: pending을 최종 처리하는 시점, 기존 앱의 표시 방법, 기능을 끈 뒤 남은 데이터 처리 -->

1.
2.
3.

## 10. AI에게 물어볼 질문

<!-- 위에서 `모름`으로 남긴 항목을 질문으로 바꾼다. 예: pending을 최종 처리하는 파일과 실행 주기를 찾아주세요. -->

1.
2.
3.

<!--
파일과 함수가 너무 많이 나오면 AI에게 이렇게 요청한다:
"세부 함수는 생략하고 책임이 바뀌는 최대 5단계로 다시 설명해 주세요."
-->

