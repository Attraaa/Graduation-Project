# 03. 구현 계획서

> AI는 계획 초안과 최종 계획 모두 이 구조를 사용한다. 계획 단계에서는 프로덕션 코드를 수정하지 않는다. `<!-- 예: ... -->` 주석은 작성 수준을 보여주는 참고 예시이며 결과 문서에 남아 있어도 렌더링되지 않는다.

## 문서 정보

<!-- 예: 결제 timeout 중복 방지 / 2026-09-16 / 사용자 결정 필요 / 이슈 #142 / ADR-0007 -->

- 작업 이름:
- 작성일:
- 계획 상태: 초안 / 사용자 결정 필요 / 승인 조건 반영 완료 / 구현 보류
- 관련 이슈:
- 관련 ADR:

## 1. 한 문장 목표

<!-- 예: 결제 결과가 불명확할 때 새 결제를 막아 같은 주문의 중복 결제를 방지한다. -->


## 2. 현재 문제

<!-- 예: 결제사는 승인했지만 서버가 응답을 잃으면 실패로 처리되어 사용자가 다시 결제할 수 있다. -->


## 3. 현재 동작

### 주요 진입점

<!-- 예: src/payments/payment.controller.ts / authorize() / 결제 버튼을 누른 사용자 -->

- 파일:
- 심볼:
- 호출 주체:

### 현재 실행 흐름

<!-- 예: 요청 수신 → 기존 결제 확인 → 결제사 승인 → 상태 저장 → 응답. 책임이 바뀌는 최대 5단계로 쓴다. -->

```text
1.
→ 2.
→ 3.
→ 4.
→ 5.
```

### 현재 데이터와 부작용

<!-- 예: 주문·결제 시도 읽기 / 결제 상태 쓰기 / 결제사 API 호출 / 성공 이벤트 발행 -->

- 읽는 데이터:
- 쓰는 데이터:
- 외부 API:
- 이벤트/메시지:
- 파일/캐시:

## 4. 원하는 동작

<!-- 예: timeout이면 실패 대신 pending으로 저장하고 같은 주문의 재요청은 기존 pending을 반환한다. 적용되지 않는 행은 `해당 없음`이라고 쓴다. -->

| 상황 | 현재 | 변경 후 |
|---|---|---|
| 정상 경로 | | |
| 주요 실패 | | |
| 중복/재시도 | | |
| 권한 없음 | | |

## 5. 범위

### 포함

<!-- 예: timeout 처리, 중복 방지, 상태 저장, 관련 테스트 -->

-

### 비범위

<!-- 예: 새 결제사, 환불 기능, 결제 화면 전체 개편 -->

-

## 6. 유지해야 하는 계약과 불변 조건

<!-- 예: 정상 승인·명확한 거절은 기존 응답을 유지하고, 기존 결제 기록은 삭제하지 않는다. 변경이 없으면 `없음`이라고 쓴다. -->

- 공개 API:
- 데이터베이스와 데이터:
- 이벤트와 메시지:
- 설정과 환경변수:
- 기존 사용자 동작:

## 7. 관련 코드 지도

<!-- 예: payment.service.ts | 결제 흐름 조정 | 예 | 불명확한 결과를 pending으로 분류 -->

| 파일/디렉터리 | 현재 책임 | 변경 필요 여부 | 예상 변경 이유 |
|---|---|---|---|
| | | | |

## 8. 제안하는 구현 단계

<!-- 단계 하나는 독립적으로 검증할 수 있게 작게 나눈다. 예: 1) 결과 타입과 단위 테스트 2) 서비스 상태 처리 3) API·화면 연결 -->

### 단계 1: 이름

- 결과:
- 변경 예상 파일:
- 구체적인 변경:
- 유지할 계약:
- 테스트와 명령:
- 성공 기준:
- 위험:
- 중지/롤백:
- 다음 단계 의존성:

### 단계 2: 이름

- 결과:
- 변경 예상 파일:
- 구체적인 변경:
- 유지할 계약:
- 테스트와 명령:
- 성공 기준:
- 위험:
- 중지/롤백:
- 다음 단계 의존성:

<!-- 단계가 더 필요하면 위 블록을 복사한다. -->

## 9. 테스트 전략

<!-- 예: 단위=결과 분류, 계약=API 응답, 통합=timeout 후 DB와 재요청, E2E=사용자 메시지 -->

| 수준 | 검증할 내용 | 예상 테스트 위치 |
|---|---|---|
| 단위 | | |
| 계약 | | |
| 통합 | | |
| E2E | | |

### 적용할 경계 및 실패 사례

- [ ] 빈 값·최소값·최대값
- [ ] 중복 요청
- [ ] 외부 시스템 실패 또는 timeout
- [ ] 부분 저장 후 실패
- [ ] 동시 요청
- [ ] 권한 없음
- [ ] 이전 버전 또는 기존 데이터
- [ ] 마이그레이션과 롤백

구체적인 사례:

-

## 10. 위험 분석

<!-- 예: 결제 성공 응답 유실 | timeout | 중복 과금 | 주문별 시도 수 지표 | pending 저장 후 새 승인 차단 -->

| 위험 | 발생 조건 | 영향 | 발견 방법 | 완화/롤백 |
|---|---|---|---|---|
| | | | | |

## 11. 배포와 호환성

<!-- 예: 서버 선배포 후 기능 플래그 10% 적용, pending 지표 관찰, 문제 시 플래그 끄기. 확인하지 못한 것은 숨기지 않는다. -->

- 배포 순서:
- 이전 버전과 동시 실행:
- feature flag:
- 데이터 마이그레이션:
- 운영 관측:
- 전체 롤백 가능 여부:

## 12. ADR 필요 여부

<!-- 예: 필요 / 중복 방지 키 저장 위치 / API·DB·재시도 계약에 장기 영향이 있기 때문 -->

- 결과: 필요 / 불필요 / 판단 보류
- 대상 결정:
- 이유:

## 13. 사용자 결정 필요

<!-- 예: pending을 공개 API에 추가할지 | 추가/기존 오류 유지 | 기존 앱 호환성 영향 | 호환 확인 후 추가 -->

| 질문 | 선택지 | 결과에 미치는 영향 | 권장안 |
|---|---|---|---|
| | | | |

## 14. 가정과 미확인 사항

<!-- 예: 모바일 앱이 새 상태를 처리하는지는 아직 확인하지 못함 -->

-

## 15. 초안 대비 변경사항

<!-- 최종 계획에서만 작성한다. 예: 사용자 요청에 따라 DB 변경과 API 변경을 별도 단계로 나눔 -->

-

## 16. 첫 구현 허용 단계

<!-- 예: 단계 1 / 결과 타입과 단위 테스트 / DB·API·화면 변경은 아직 승인되지 않음 -->

- 단계 번호:
- 단계 이름:
- 구현 전 필요한 승인:

## 최종 상태 선언

```text
계획 상태:
첫 구현 허용 단계:
ADR 필요 여부:
남은 사용자 결정:
```

